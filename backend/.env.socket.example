# Socket.IO Configuration
SOCKET_IO_CORS_ORIGIN=http://localhost:3000

# Redis Configuration (optional but recommended for scalability)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=

# Note: If REDIS_URL is not provided, Socket.IO will use the default in-memory adapter
# This is fine for development but not recommended for production with multiple server instances
